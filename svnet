#!/usr/bin/env bash
set -uo pipefail

if command -v mikrotik-vpn >/dev/null 2>&1; then
  exec mikrotik-vpn "$@"
fi

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/mikrotik-vpn" "$@"
